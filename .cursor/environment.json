{
  "name": "trace-server (Axum + Postgres)",
  "install": "bash .cursor/install.sh",
  "start": "bash .cursor/start.sh",
  "terminals": [
    {
      "name": "trace-server",
      "description": "The Axum HTTP backend on http://localhost:8080, connected to the local Postgres 'trace' database. Endpoints: /health, /ready, /api/v1/users, /api/v1/journeys.",
      "command": "cd src/trace-server && DATABASE_URL=postgres://trace:trace@localhost:5432/trace RUST_LOG=trace_server=info,tower_http=info cargo run --locked"
    }
  ],
  "ports": [
    {
      "name": "trace-server",
      "port": 8080
    }
  ]
}
